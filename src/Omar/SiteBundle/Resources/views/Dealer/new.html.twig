{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Dealer creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('dealer') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
