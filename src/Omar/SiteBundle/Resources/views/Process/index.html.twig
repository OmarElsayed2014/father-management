{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Process list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Price</th>
                <th>Remain</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('process_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.Date %}{{ entity.Date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.price }}</td>
                <td>{{ entity.remain }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('process_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('process_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('process_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
